# Executable reference code

The maintained implementation is under `src/locked_saturation/`. Run `pip install -e .`, then `pytest -q` and `python scripts/run_reference_reproduction.py`. See `REPRODUCIBILITY_STATUS.md` for the exact boundary between implemented equations and unavailable original artifacts.
